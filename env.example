# Database Configuration
# For local development, you can use SQLite (current setup)
DATABASE_URL="file:./dev.db"

# For production deployment on Vercel, use a cloud database
# Uncomment and configure one of the following:

# PostgreSQL (Recommended for production)
# DATABASE_URL="postgresql://username:password@hostname:port/database"

# MySQL
# DATABASE_URL="mysql://username:password@hostname:port/database"

# MongoDB
# DATABASE_URL="mongodb://username:password@hostname:port/database"

# Supabase PostgreSQL (if you want to add auth back later)
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"
# NEXT_PUBLIC_SUPABASE_URL="https://[YOUR-PROJECT-REF].supabase.co"
# NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Production URL (set this in Vercel dashboard)
# NEXT_PUBLIC_APP_URL="https://your-app-name.vercel.app"
